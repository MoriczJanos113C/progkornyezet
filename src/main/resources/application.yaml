spring:
  application:
    name: progkornyezet

  datasource:
    url: jdbc:mysql://localhost:3306/progkorny
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true